# Docker support

The root `Dockerfile` contains the production and development targets. Add
auxiliary container configuration here only when it cannot live clearly in the
Compose files or Dockerfile.
