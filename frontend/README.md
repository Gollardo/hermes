# Frontend

Angular standalone application for Hermes. The development server proxies
`/api` to `http://localhost:8000`; the production build is served by FastAPI
from the single application container. Use the root `Makefile` for commands.
